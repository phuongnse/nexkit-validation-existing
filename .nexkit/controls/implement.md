# Label service implementation

Implement the exact approved issue in this Python standard-library service. Read
the existing service, tests and README. Preserve established behavior outside the
request. Consume prior test/reviewer/human feedback from the supplied context.
Add meaningful regression tests and update relevant README guidance. Run all
checks the agent sandbox permits, and report unavailable socket checks honestly;
the separate hosted verification jobs run the actual HTTP/package suites.
